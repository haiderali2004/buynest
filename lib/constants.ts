/** Orders at or above this subtotal (in PKR) ship for free. */
export const FREE_SHIPPING_THRESHOLD = 4999;

/** Flat shipping rate (in PKR) applied below the free-shipping threshold. */
export const FLAT_SHIPPING_RATE = 250;

/** Default currency for the storefront, matching the database schema's default. */
export const DEFAULT_CURRENCY = "PKR";
